# GoReleaser config — builds cross-platform static binaries (no cgo) and
# publishes a GitHub Release + Scoop/Homebrew manifests on each `v*` tag.
# Docs: https://goreleaser.com
version: 2

project_name: nem

before:
  hooks:
    - go mod tidy

builds:
  - id: nem
    main: ./cmd/nem
    binary: nem
    env:
      - CGO_ENABLED=0 # nem es Go puro (modernc sqlite): binario estático
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/Dieg0Code/nem/internal/cli.version={{ .Version }}

archives:
  - id: nem
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  use: git
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "Merge pull request"

# Windows: scoop install nem (tras: scoop bucket add Dieg0Code https://github.com/Dieg0Code/scoop-bucket)
scoops:
  - repository:
      owner: Dieg0Code
      name: scoop-bucket
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/Dieg0Code/nem
    description: Git-style versioned memory for AI coding agents (Claude Code, Codex & Antigravity).
    license: MIT

# macOS/Linux: brew install Dieg0Code/homebrew-tap/nem
homebrew_casks:
  - repository:
      owner: Dieg0Code
      name: homebrew-tap
      token: "{{ .Env.TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/Dieg0Code/nem
    description: Git-style versioned memory for AI coding agents (Claude Code, Codex & Antigravity).
    # Quita la cuarentena de macOS para que el binario sin firmar pueda correr.
    hooks:
      post:
        install: |
          if system_command("/usr/bin/xattr", args: ["-h"]).exit_status == 0
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/nem"]
          end

release:
  github:
    owner: Dieg0Code
    name: nem
  draft: false
